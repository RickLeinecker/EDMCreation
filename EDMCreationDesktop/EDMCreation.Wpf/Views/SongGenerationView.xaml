<views:MvxWpfView x:Class="EDMCreation.Wpf.Views.SongGenerationView"
             xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"
             xmlns:mvx="clr-namespace:MvvmCross.Platforms.Wpf.Binding;assembly=MvvmCross.Platforms.Wpf"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EDMCreation.Wpf.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20" />
            <ColumnDefinition />
            <ColumnDefinition />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="20" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20" />
            <RowDefinition Height="auto" />
            <RowDefinition />
            <RowDefinition Height="auto" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="2" Grid.Column="1">
            <StackPanel Orientation="Horizontal">
                <Button Style="{DynamicResource MaterialDesignIconButton}" mvx:Bi.nd="Command PlayCommand">
                    <materialDesign:PackIcon Kind="Play" />
                </Button>
                <Label Content="[seekbar]" />
                <Label Content="Song 1" />
                <Button Style="{DynamicResource MaterialDesignIconButton}" mvx:Bi.nd="Command LikeCommand">
                    <materialDesign:PackIcon Kind="Like" />
                </Button>
                <Button Style="{DynamicResource MaterialDesignIconButton}" mvx:Bi.nd="Command DislikeCommand">
                    <materialDesign:PackIcon Kind="Dislike" />
                </Button>
                <Button Style="{DynamicResource MaterialDesignIconButton}" mvx:Bi.nd="Command SaveCommand">
                    <materialDesign:PackIcon Kind="Floppy" />
                </Button>
            </StackPanel>
        </StackPanel>

    </Grid>
</views:MvxWpfView>
